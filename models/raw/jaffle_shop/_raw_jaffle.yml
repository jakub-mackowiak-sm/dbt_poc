models:

  - name: raw_customers
    description: Customers raw
    columns:
      - name: customer_id
        data_tests:
          - not_null
          - unique

  - name: raw_orders
    description: Orders raw
    columns:
      - name: order_id
        data_tests:
          - not_null
          - unique
      - name: customer_id
        data_tests:
          - not_null
          - relationships:
              field: customer_id
              to: ref('raw_customers')
      - name: order_status
        description: from a list of accepted values "{{ doc('order_status') }}" 
        data_tests:
          - accepted_values:
              values:
                - completed
                - return_pending
                - placed
                - shipped
                - returned